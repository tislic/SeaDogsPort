---
layout: default
title: Обсуждение
permalink: /обсуждение/
---

<div class="panel post" style="padding: 40px; background:rgba(255,253,248,.89);">

  <h1 style="font-size:2rem; font-weight:normal; margin:0 0 20px;">Обсуждение</h1>

  <div class="post-content" style="color:#4d5357; line-height:1.75;">
    Здесь можно задать вопрос, сообщить об ошибке или обсудить разработку проекта.
  </div>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/gitalk@1/dist/gitalk.css">

  <div id="gitalk-container" style="margin-top:30px;"></div>

  <script src="https://cdn.jsdelivr.net/npm/gitalk@1/dist/gitalk.min.js"></script>
  <script>
    const gitalk = new Gitalk({
      clientID: 'Ov23liWdlLA0zxfwDGDn',
      clientSecret: 'bf419a2edf4599e94679b8c7cdac736b3a2e7034',
      repo: 'SeaDogsPort',
      owner: 'tislic',
      admin: ['tislic'],
      id: 'discussion',
      title: 'Обсуждение',
      language: 'ru',
      distractionFreeMode: false,
      createIssueManually: false,
      proxy: 'https://cors-anywhere.azm.workers.dev/https://github.com/login/oauth/access_token'
    });

    /*
     * Gitalk 1.x берёт redirect_uri из window.location.href.
     * На странице /обсуждение/ это даёт двойное percent-encoding
     * кириллического пути. Поэтому перехватываем клик по кнопке
     * авторизации и отправляем OAuth на отдельный ASCII callback.
     */
    gitalk.render('gitalk-container');

    document.addEventListener('click', function (event) {
      const button = event.target.closest('.gt-btn-login');
      if (!button) return;

      event.preventDefault();
      event.stopPropagation();

      const params = new URLSearchParams({
        client_id: 'Ov23liWdlLA0zxfwDGDn',
        redirect_uri: 'https://contrabandist.ru/gitalk-callback/',
        scope: 'public_repo'
      });

      window.location.href =
        'https://github.com/login/oauth/authorize?' + params.toString();
    }, true);
  </script>

</div>
